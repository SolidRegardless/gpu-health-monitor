# GPU Health Monitor - Environment Configuration
# Copy this file to .env and customize values

# Grafana Admin Credentials
# IMPORTANT: Change these defaults before deploying to production!
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme123

# Database Credentials (must match docker-compose.yml)
POSTGRES_USER=gpu_monitor
POSTGRES_PASSWORD=gpu_monitor_secret
POSTGRES_DB=gpu_health

# Optional: Override default ports
# GRAFANA_PORT=3000
# TIMESCALEDB_PORT=5432
# API_PORT=8000
